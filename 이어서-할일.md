# forMe — 작업 이어하기 메모 (2026-09-10)

학원 컴퓨터에서 여기까지 함. 집에서 이어서 하면 됨.

---

## 지금까지 상황

### 증상 3개
1. 웹(`pnpm dev`)에서 이름 입력 후 → 다시 이름 입력 화면으로 되돌아오는 무한 루프
2. 홈 화면에 추천 활동 버블이 안 떠다님
3. 배포한 버전(핸드폰/앱)이 흰 화면만 나옴

### 원인
- **1, 2번**: Supabase에서 `permission denied for table profiles` 에러.
  → 테이블에 대한 역할 권한(GRANT)이 안 걸려 있었음. RLS 정책도 확실히 안 걸려 있었음.
  → **해결 완료** (아래 SQL 실행함)
- **3번**: 배포 서버(Vercel)에 Supabase 열쇠 값(환경변수)이 안 들어가 있음.
  웹은 내 컴퓨터의 `.env.local` 파일을 읽지만, 그 파일은 git/Vercel에 안 올라감.
  빌드된 번들 안에서 URL/키가 빈 문자열(`l_="",u_=""`)이라 `supabase.js`가
  로드 시점에 throw → React가 아예 안 뜸 → 흰 화면.
  → **아직 안 함. 집에서 해야 할 일.**

---

## 이미 한 것

### A. Supabase SQL Editor에서 실행함 (프로젝트: plvaqwehpnksieaxtuol)

```sql
grant usage on schema public to anon, authenticated;
grant all on all tables in schema public to anon, authenticated;
grant all on all sequences in schema public to anon, authenticated;
alter default privileges in schema public grant all on tables to anon, authenticated;

alter table public.profiles   enable row level security;
alter table public.activities enable row level security;
alter table public.records    enable row level security;

drop policy if exists "own profiles" on public.profiles;
create policy "own profiles" on public.profiles
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);

drop policy if exists "own activities" on public.activities;
create policy "own activities" on public.activities
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);

drop policy if exists "own records" on public.records;
create policy "own records" on public.records
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);
```

→ 이후 웹에서 이름 저장 → 홈 → 활동 버블 뜨는 것 확인함. **웹은 정상 작동.**

### B. 코드 수정함 (아직 커밋 안 됨!)

| 파일 | 바뀐 내용 |
| --- | --- |
| `src/context/AppContext.jsx` | `saveProfile`를 `async`로 바꾸고, 저장 실패 시 에러를 `throw`하도록 수정 (예전엔 조용히 삼킴) |
| `src/screens/Onboarding.jsx` | `start()`가 저장 성공해야 홈으로 이동. 실패하면 버튼 위에 빨간 글씨로 원인 표시. 사진 업로드 에러도 화면에 표시 (`onFile`의 `catch {}` 제거) |
| `supabase/schema.sql` | GRANT 섹션 추가 (다음에 새로 세팅할 때 또 안 막히게) |

**집에서 할 것:** 이 변경사항을 커밋 + 푸시.
```bash
git add -A
git commit -m "프로필 저장 실패를 화면에 표시 + schema에 GRANT 추가"
git push
```
(Vercel이 GitHub 연동이면 push하면 자동 재배포됨)

---

## 집에서 해야 할 일: 배포 흰 화면 고치기

### 1. 값 2개 준비
프로젝트 폴더의 **`.env.local`** 파일을 메모장으로 열기. 이렇게 생김:
```
VITE_SUPABASE_URL=https://plvaqwehpnksieaxtuol.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbG..... (엄청 긴 문자열)
```
※ 집 컴퓨터에 이 파일이 없으면: Supabase 사이트 → 프로젝트 → Settings → API 에서
  `Project URL` 과 `anon` `public` key 를 복사하면 됨.

### 2. Vercel에 환경변수 넣기
1. vercel.com 로그인 → 다이어리 프로젝트 클릭
2. 위쪽 탭 **Settings** → 왼쪽 메뉴 **Environment Variables**
3. 2개 추가:
   - Key: `VITE_SUPABASE_URL`  / Value: `.env.local`의 `https://...supabase.co` 값
   - Key: `VITE_SUPABASE_ANON_KEY` / Value: `.env.local`의 `eyJhbG...` 긴 문자열
4. 각각 **Production / Preview / Development 체크박스 3개 다 체크**
5. **Save**

### 3. 다시 배포
- 위쪽 탭 **Deployments** → 맨 위 항목 오른쪽 **⋯** → **Redeploy** → 팝업에서 다시 **Redeploy**
- 1~2분 기다림
- (B의 `git push`를 했으면 그걸로도 자동 재배포되니 이 단계 생략 가능)

### 4. 핸드폰에서 확인
- 예전에 **홈 화면에 추가**해둔 아이콘이 있으면 옛날 흰 화면이 저장돼 있음 → 아이콘 삭제하고 브라우저에서 주소 다시 열기
- 그냥 브라우저로 접속했던 거면 새로고침만
- **되면 끝!** 안 되면 핸드폰 브라우저에서 안 되는 화면 상태 + 에러 메시지 확인

### 5. (선택) Supabase 인증 URL 설정
이메일 인증/비번 재설정 링크가 배포 도메인 가리키게 하려면:
- Supabase → Authentication → URL Configuration
- Site URL: `https://<배포도메인>.vercel.app`
- Redirect URLs: `https://<배포도메인>.vercel.app/**`
- ※ `Confirm email` 꺼놓고 비번 로그인만 쓰면 없어도 동작함

---

## 원인 요약 (왜 이랬는지)

- **웹은 되는데 배포는 흰 화면** = `VITE_` 환경변수는 **빌드 시점에 코드 안에 박힘**.
  로컬은 `.env.local`을 읽어서 박히지만, Vercel은 그 파일이 없어서 빈 값으로 빌드됨.
  `src/lib/supabase.js`가 값 없으면 즉시 `throw` → 화면 전체가 안 뜸.
  → Vercel에 환경변수 넣고 **재배포**하면 해결 (값 바꾸면 재배포 필수).
